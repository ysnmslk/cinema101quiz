
name: myapp
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase (Uyumlu versiyon seti)
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  google_sign_in: ^6.2.1

  # State Management
  provider: ^6.1.2

  # UI & Icons
  cupertino_icons: ^1.0.8
  go_router: ^17.0.0
  google_fonts: ^6.2.1
  font_awesome_flutter: ^10.8.0
  transparent_image: ^2.0.1
  intl: ^0.20.2
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
